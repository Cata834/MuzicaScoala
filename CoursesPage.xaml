<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:syncfusion="clr-namespace:Syncfusion.Maui.Calendar;assembly=Syncfusion.Maui.Calendar"
             x:Class="MuzicaScoala.CoursesPage">

    <StackLayout Padding="20">
        <Label Text="Selectează data cursului"
               FontSize="24"
               HorizontalOptions="Center" />

        <!-- Calendarul Syncfusion -->
        <syncfusion:SfCalendar x:Name="calendar" SelectionChanged="OnSelectionChanged" SelectionMode="Single"/>

        <Button Text="Adaugă Curs"
                Clicked="OnAddCourseClicked"
                HorizontalOptions="Center"
                VerticalOptions="CenterAndExpand" />

        <Label Text="Courses" FontSize="24" HorizontalOptions="CenterAndExpand" />

        <ListView x:Name="CoursesListView" ItemTapped="OnCourseTapped">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Name}" Detail="{Binding InstructorName}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </StackLayout>
</ContentPage>
